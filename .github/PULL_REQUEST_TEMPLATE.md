## Summary

<!-- What changes, and why. For behavior changes: before vs. after. -->

## Semantics checklist

- [ ] No language semantics changed, **or** SPEC.md, the reference parser,
      affected renderers, and golden files (`npm run golden:update`) are all
      updated together
- [ ] Commands are matched with anchored patterns (no substring dispatch)
- [ ] `npm test` passes

## Test plan

<!-- New/updated tests, and anything verified manually (which renderer, what stream). -->
